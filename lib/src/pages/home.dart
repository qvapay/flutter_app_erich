import 'package:flutter/material.dart';

class Home extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: const Text('Welcome to QvaPay'),
        ),
        body: Column(
          children: [
            // Current Balance Card
            // Some promo and cards/services
            // Latest transactions
          ],
        ));
  }
}
